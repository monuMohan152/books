<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Blog CMS</title>
  <style>
    body { font-family: Arial, sans-serif; margin: 2rem; }
    .blog-post { max-width: 800px; margin: auto; }
    h1 { font-size: 2rem; margin-bottom: 0.5rem; }
    .meta { color: gray; font-size: 0.9rem; margin-bottom: 1rem; }
  </style>
</head>
<body>
  <div id="blog" class="blog-post">
    <p>Loading post...</p>
  </div>

  <script>
    const csvUrl = "https://raw.githubusercontent.com/monuMohan152/books/refs/heads/main/BlogDataNew.csv";

    function getSlugFromPath() {
      const path = window.location.pathname;
      return path.startsWith("/") ? path.slice(1) : path;
    }

    async function loadCSV(url) {
      const res = await fetch(url);
      const text = await res.text();
      return parseCSV(text);
    }

    function parseCSV(text) {
      const [header, ...lines] = text.trim().split("\n");
      const keys = header.split(",");
      return lines.map(line => {
        const values = line.match(/(".*?"|[^",]+)(?=\s*,|\s*$)/g).map(v => v.replace(/^"|"$/g, ""));
        return Object.fromEntries(keys.map((k, i) => [k.trim(), values[i]?.trim() || ""]));
      });
    }

    async function renderPost() {
      const slug = getSlugFromPath();
      const posts = await loadCSV(csvUrl);
      const post = posts.find(p => p.slug === slug);

      const blogEl = document.getElementById("blog");
      if (!post) {
        blogEl.innerHTML = `<h1>404 - Post Not Found</h1>`;
        return;
      }

      blogEl.innerHTML = `
        <h1>${post.title}</h1>
        <div class="meta">By ${post.author} on ${post.date}</div>
        <div class="content">${post.content}</div>
      `;
    }

    renderPost();
  </script>
</body>
</html>
